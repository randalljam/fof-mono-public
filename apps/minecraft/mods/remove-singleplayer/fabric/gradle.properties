# Remove-Singleplayer mod metadata for the Fabric gradle root. KEEP IN SYNC with the
# forge/ gradle root's gradle.properties — mod_version and archives_base_name should
# match across both. See minecraft/mods/CLAUDE.md § "Cross-loader-root metadata sync".

org.gradle.jvmargs=-Xmx1G
org.gradle.parallel=true

mod_version=0.3.0
maven_group=com.kidgames
archives_base_name=remove-singleplayer
