# MathQuest mod metadata. Per-target Minecraft / loader / mappings versions live in
# each target's own gradle.properties under targets/<target>/gradle.properties.

org.gradle.jvmargs=-Xmx1G
org.gradle.parallel=true
org.gradle.java.installations.paths=/usr/local/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home,/usr/local/opt/openjdk/libexec/openjdk.jdk/Contents/Home

# Mod identity (shared across all targets)
mod_version=1.25.4
maven_group=com.kidgames
archives_base_name=mathquest
